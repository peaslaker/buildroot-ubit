#!/bin/ash

	reboot() {
		touch /tmp/reboot
	}

. /usr/lib/ubit/profile/full

touch .ubit.once
cat .ubit.once > .ubit.last 2>/dev/null
rm .ubit.once
[ -e .ubit.last ] && {
	. .ubit.last
}
[ -e .ubit.action ] && {
	. .ubit.action
}

