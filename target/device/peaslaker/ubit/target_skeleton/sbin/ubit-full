#!/bin/ash

	reboot() {
		touch /tmp/reboot
	}

export ENV=/usr/lib/ubit/profile/full
. ${ENV}

touch .ubit.once
cat .ubit.once > .ubit.last 2>/dev/null
rm .ubit.once
[ -e .ubit.last ] && {
	. .ubit.last
}
[ -e .ubit.action ] && {
	. .ubit.action
}

